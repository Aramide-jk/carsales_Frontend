version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18  # or 20 for newer version
    commands:
      - echo "Installing Node.js and npm..."
      - node --version
      - npm --version
  pre_build:
    commands:
      - echo "Running npm ci..."
      - npm ci
  build:
    commands:
      - echo "Build started on `date`"
      - # Your build commands here
