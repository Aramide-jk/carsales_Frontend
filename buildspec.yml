version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm ci

  build:
    commands:
      - echo Running Vite build...
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: dist
